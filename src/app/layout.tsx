import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
	variable: "--font-geist-sans",
	subsets: ["latin"],
});

const geistMono = Geist_Mono({
	variable: "--font-geist-mono",
	subsets: ["latin"],
});

export const metadata: Metadata = {
	title: "Mood Fast - Your Personal Companion for Emotional Wellness",
	description:
		"Track your mood in seconds, discover patterns, and get personalized activities to feel your best every day. Download free on the App Store.",
	keywords: [
		"mood tracker app",
		"emotional wellness app",
		"mental health tracker",
		"mood journal",
		"daily mood log",
	],
};

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang="en">
			<body
				className={`${geistSans.variable} ${geistMono.variable} antialiased`}
			>
				{children}
			</body>
		</html>
	);
}
